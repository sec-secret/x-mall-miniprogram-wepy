import HtmlToJson from '../wxParse/html2json.js';
export default class CPParse {
    static parse(nodeString){
     let translatedNode = HtmlToJson.html2json(data)
      retrun translatedNode;
    }
}
